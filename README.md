# 📅 CampusMate - 學霸課表 (v2.2)

**CampusMate** 是一個專為學生設計的現代化課表與成績管理工具。結合 PWA 技術，讓您能直接安裝於手機桌面。本版本新增了資料視覺化分析與課程性質標記功能，幫助您更精確地掌握學習進度。

## ✨ 最新功能 (v2.2 更新)

### 📈 學習視覺化分析
* **成績趨勢圖**：整合 **Chart.js**，自動繪製歷年學期成績折線圖，進步退步一目了然。
* **學分進度條**：針對大學生模式，首頁顯示畢業學分達成率。
* **自訂畢業門檻**：可於「設定」頁面自訂系所規定的畢業總學分 (例如 128, 140 等)，進度條將自動重新計算。

### 🏷️ 課程性質標記
* **必修/選修標籤**：新增課程與成績時，可選擇「必修、選修、通識、體育」等屬性。
* **彩色標記**：課表中會依據課程性質顯示不同顏色的標籤 (紅/綠/橘)，重要課程不錯過。

---

## 🚀 主要特色

### 1. 📝 智慧課表管理
* **每週總覽**：直覺式的週一至週五分頁切換。
* **詳細資訊**：記錄節次、時間、科目、教室位置、老師及**課程性質**。
* **PWA 支援**：支援 Add to Home Screen，提供如同原生 App 的流暢體驗。

### 2. 📊 雙模式成績試算
* **🎓 大學生模式**：支援學分輸入，自動計算**加權 GPA** 與畢業學分進度。
* **🏫 高中生模式**：專注於分數，自動計算算術平均。

### 3. 🗓️ 多學期檔案系統
* **完整記錄**：支援建立與切換多個學期檔案（如 113-1, 113-2）。
* **資料雲端化**：整合 Firebase Auth，登入後資料跟著帳號走 (需連網)。

---

## 📂 專案結構說明

本專案採用模組化 JavaScript 開發，主要檔案功能如下：

* **`index.html`**：應用程式主頁面，包含所有視圖 (Views) 與彈出視窗 (Modals)。
* **`js/main.js`**：程式入口點，負責初始化與監聽使用者登入狀態。
* **`js/state.js`**：管理全域變數 (如 `currentSemester`, `graduationTarget` 等)。
* **`js/data.js`**：資料存取核心，負責 `localStorage` 的讀寫與 Firebase 資料同步。
* **`js/ui.js`**：介面控制，負責頁面切換 (Tabs) 與通用 UI 邏輯。
* **`js/course.js`**：**課表邏輯**，包含課程的新增、編輯、刪除與**性質標籤**處理。
* **`js/grade.js`**：**成績與分析邏輯**，包含 GPA 計算、**Chart.js 圖表繪製**與**學分進度條**控制。
* **`js/semester.js`**：學期管理，負責新學期的建立與切換。
* **`js/firebase.js`**：Firebase SDK 初始化設定。
* **`css/`**：包含 `base.css` (變數), `layout.css` (佈局), `components.css` (元件), `pages.css` (頁面樣式)。

---

## 📖 使用說明

1.  **初次設定**：
    * 登入後請先選擇身分（高中/大學）。
    * 若為大學生，請至「⚙️ 設定」頁面輸入您的**畢業學分目標** (預設 128)。

2.  **新增課程/成績**：
    * 點擊「+」按鈕新增時，記得選擇**課程性質** (必修/選修)，系統將自動標記顏色。

3.  **查看分析**：
    * 回到首頁下方，即可看到最新的**成績趨勢圖**與**學分達成率**。

---

## 🛠️ 技術棧

* **Frontend**: HTML5, CSS3, Vanilla JavaScript
* **Chart**: Chart.js (CDN)
* **Database/Auth**: Google Firebase
* **Mobile**: Progressive Web App (Manifest + Service Worker)

---

> 版本：v2.2.0
> 開發者：CampusMate Team
